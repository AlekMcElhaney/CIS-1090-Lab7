function search(needle, haystack) {
    let start = 0;
    let end = haystack.length;
    while (start < end) {




        function addWord(needle, haystack) {
            let start = 0;
            let end = haystack.length;

            for let i = start; i < end; i++) {
                if (needle == haystack[i]) {
                    return true;

                }
                return false;
            }

            export default function (word) {
                return linearSearch(word, words);
            }

        

    


